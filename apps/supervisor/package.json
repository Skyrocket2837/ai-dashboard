{
  "name": "@ai-dashboard/supervisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/supervisor.mjs",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/supervisor.mjs",
    "start": "node --env-file-if-exists=.env src/supervisor.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-dashboard/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
